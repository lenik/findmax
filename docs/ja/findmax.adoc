= findmax(1)
{project-author} <{project-email}>
v{project-version}, {project-year}
:doctype: manpage
:manmanual: User Commands
:mansource: findmax
:man-linkstyle: blue R <>
:nofooter:

== NAME

findmax - 最大値クエリ向けに最適化された高速ファイル検索ユーティリティ

== SYNOPSIS

*findmax* [_OPTIONS_] [_FILE_...]

== DESCRIPTION

*findmax* は指定条件の最大値を持つファイルを探すための高速ユーティリティで、O(1) クエリ向けに最適化されています。更新/アクセス/作成時刻、サイズ、名前で検索でき、出力形式やフィルタにも対応します。

== OPTIONS

*-R*, *--recursive*::
  ディレクトリを再帰的に検索します。
*-r*, *--reverse*::
  逆順に並べます（最古/最小を先に）。
*-u*::
  アクセス時刻 (atime) で並べます。
*-c*::
  メタデータ変更時刻 (ctime) で並べます。
*-t*::
  更新時刻 (mtime) で並べます。これが既定です。
*-S*::
  ファイルサイズで並べます。
*-n*, *--name*::
  現在のロケール設定でファイル名順に並べます。
*-f*, *--file-only*::
  通常ファイルのみ表示します。
*-d*, *--dir-only*::
  ディレクトリのみ表示します。
*-L*, *--dereference*::
  シンボリックリンクをたどります (lstat() の代わりに stat())。
*-F*, *--format* _FMT_::
  カスタム出力形式を使います。詳細は FORMAT 節を参照。
*-_NUM_*::
  既定の 1 件ではなく上位 NUM 件を表示します。
*-v*, *--verbose*::
  繰り返すとより詳細なログになります。
*-q*, *--quiet*::
  ログを減らし、致命的でないエラーを抑制します。
**--time**=__WORD__::
  並べ替えに使うタイムスタンプを選びます:
  +
  *atime*, *access*, *use*;;
    アクセス時刻
  *ctime*, *status*;;
    メタデータ変更時刻
  *mtime*, *modification*;;
    更新時刻（既定）
  *birth*, *creation*;;
    作成時刻（対応時）
**--maxdepth**=__NUM__::
  ディレクトリ探索の深さを NUM に制限します。
*-h*, *--help*::
  このヘルプを表示して終了します。
*--version*::
  バージョン情報を表示して終了します。

== FORMAT

The format string supports the following specifiers:

=== File Information

*%n*::
  File name
*%N*::
  Quoted file name with dereference if symbolic link
*%s*::
  Total size in bytes
*%F*::
  File type (regular file, directory, etc.)
*%f*::
  Raw mode in hexadecimal
*%a*::
  Permission bits in octal
*%A*::
  Permission bits and file type in human readable form

=== Ownership

*%u*::
  User ID of owner
*%U*::
  User name of owner
*%g*::
  Group ID of owner
*%G*::
  Group name of owner

=== Timestamps

*%x*::
  Time of last access, human-readable
*%X*::
  Time of last access, seconds since Epoch
*%y*::
  Time of last data modification, human-readable
*%Y*::
  Time of last data modification, seconds since Epoch
*%z*::
  Time of last status change, human-readable
*%Z*::
  Time of last status change, seconds since Epoch
*%w*::
  Time of file birth, human-readable (- if unknown)
*%W*::
  Time of file birth, seconds since Epoch (0 if unknown)

=== System Information

*%d*::
  Device number in decimal
*%D*::
  Device number in hexadecimal
*%i*::
  Inode number
*%h*::
  Number of hard links
*%b*::
  Number of blocks allocated
*%B*::
  Size in bytes of each block (usually 512)

== EXAMPLES

Find the most recently modified file in current directory:::
  *findmax -t .*
Find the oldest file recursively with timestamp:::
  *findmax -r -t -R -F %n %y /home/user*
Find the 5 largest files:::
  *findmax -S -R -5 /var/log*
Find newest regular file only:::
  *findmax -t -f -R /etc*
Find files with detailed information:::
  *findmax -t -F %n (%s bytes) %U:%G %y /tmp*
Limit search depth to 2 levels:::
  *findmax -t -R --maxdepth=2 /usr/share*

== PERFORMANCE

*findmax* は最小ヒープで上位 N 件だけを保持し、最大値だけが必要なときに全ファイルのソートを避けます。典型的な用途ではほぼ O(1) の性能です。

== EXIT STATUS

*0*::
  成功
*1*::
  一般エラー（無効な引数、ファイルアクセスエラーなど）

== AUTHOR

Lenik <findmax@bodz.net> によって書かれました。

== COPYRIGHT

Copyright (C) 2026 Lenik。これはフリーソフトウェアです。複製条件はソースを参照してください。保証はありません。

== SEE ALSO

*find*(1), *ls*(1), *stat*(1), *locate*(1)
